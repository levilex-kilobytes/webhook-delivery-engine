describe("EventService", () => {
  it("should create a new event");
  it("should generate a unique event id");
  it("should mark an event as delivered after a successful webhook");
  it("should record a successful delivery attempt");
  it("should record a failed delivery attempt");
});
