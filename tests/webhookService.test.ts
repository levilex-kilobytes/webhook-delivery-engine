describe("WebhookService", () => {
  it("should send a webhook successfully");
  it("should return a failed attempt when the endpoint returns an error");
  it("should return a failed attempt when the request times out");
});
